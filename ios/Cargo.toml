[package]
name = "word_wheel_ios"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
eframe = { workspace = true }
egui = { workspace = true }
word_wheel_core = { path = "../core" }
