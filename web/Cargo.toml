[package]
name = "word_wheel_web"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
eframe = { workspace = true }
egui = { workspace = true }
word_wheel_core = { path = "../core" }
wasm-bindgen-futures = "0.4"
