<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordWheel.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.WordWheel.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdMob App ID. REQUIRED by the Mobile Ads SDK — the app
             crashes on MobileAds.initialize() if missing/malformed.

             ⚠️ THIS IS GOOGLE'S TEST APP ID — serves test ads only,
             no revenue. Before a production release, replace with the
             real App ID from the AdMob console:
               AdMob → Apps → Word Wheel → App settings → App ID
               format: ca-app-pub-4602595932540837~XXXXXXXXXX
             (publisher prefix 4602595932540837 is already ours; only
             the ~XXXXXXXXXX app suffix differs). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

    </application>

</manifest>
