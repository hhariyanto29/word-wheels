<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WordWheel" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme (pre-API 31) -->
    <style name="Theme.WordWheel.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_top</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.WordWheel</item>
    </style>
</resources>
